<UserControl
    x:Class="KodiRemote.View.UserControls.ConnectionState"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KodiRemote.View.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Grid>
        <Image x:Name="ConnectionStateImage" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch"  Margin="15" Source="{Binding CurrentConnectionStateImage}" Tapped="ConnectionStateImage_Tapped"/>
        <ProgressRing HorizontalAlignment="Stretch" VerticalAlignment="Stretch" IsActive="{Binding UpdateRunning}" />
    </Grid>
</UserControl>
